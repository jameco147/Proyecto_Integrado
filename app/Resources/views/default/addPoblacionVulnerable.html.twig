{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% extends 'base.html.twig' %}


{% block title %}
      Población vulnerable
{% endblock %}


{% block body %}
      <div class="container mb-5">
            <div class="row">
                  <div class="col-12">
                        <h1 class="tituloFormulario text-center">Población vulnerable</h1>
                  </div>
            </div>
      </div>
      <div class="container">
            <div class="row d-flex justify-content-center">
                  <div class="col-6 formulario">
                        {{form_start(form)}}
                        {{form_widget(form)}}
                        {{form_end(form)}}
                        <button class="btn botonjove">
                              <a class="text-white sinsub" href="{{(path('homepage',{'proy':proy, 'estado':"finalizar"}))}}">Finalizar Proyecto</a>
                        </button>
                  </div>
            </div>
      </div>
{% endblock %}